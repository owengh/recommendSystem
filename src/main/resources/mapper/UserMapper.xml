<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
  
<mapper namespace="com.cognition.bl.dao.UserMapper">  
    <select id="findUserById" parameterType="String" resultType="com.cognition.bl.domain.SalesOrder">  
       SELECT id,order_no,PAY_AMOUNT FROM sales_order where order_no = #{orderNO}  
    </select>  
</mapper>  